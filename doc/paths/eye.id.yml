put:
  tags:
    - Eye
  summary: め編集
  parameters:
    - $ref: '../components/parameters/eye.id.yml'
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../components/schemas/eye.yml'
  responses:
    '200':
      description: 編集しため
      content:
        application/json:
          schema:
            items:
              $ref: '../components/schemas/eye.yml'

delete:
  tags:
    - Eye
  summary: め削除
  parameters:
    - $ref: '../components/parameters/eye.id.yml'
  responses:
    '200':
      description: 削除しため
      content:
        application/json:
          schema:
            items:
              $ref: '../components/schemas/eye.yml'
